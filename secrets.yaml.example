# Copy this file to secrets.yaml and fill in real values.
# secrets.yaml itself is gitignored and must never be committed.
#
# Use it in other YAML files like: api_key: !secret some_api_key

some_password: welcome
